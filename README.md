# 2026-04-22

# 🤖 智慧聊天機器人（Smart Chatbot）

一個基於 **FastAPI + SQLite** 的智慧聊天機器人 Web 應用程式，支援多輪對話、多聊天室管理、檔案上傳與記憶機制。

---

## 📋 目錄

- [功能特色](#功能特色)
- [技術架構](#技術架構)
- [專案結構](#專案結構)
- [安裝與執行](#安裝與執行)
- [使用說明](#使用說明)
- [API 文件](#api-文件)
- [開發文件](#開發文件)
- [未來規劃](#未來規劃)

---

## ✨ 功能特色

| 功能 | 說明 |
|------|------|
| 💬 多輪對話 | 支援建立多個聊天室，每個聊天室維持獨立的對話上下文 |
| 📁 檔案上傳 | 支援上傳圖片（jpg/png/gif）與文件（pdf/txt/docx），單檔上限 10MB |
| 🧠 記憶機制 | 儲存使用者偏好（顯示名稱、語言、回覆風格），跨聊天室持續有效 |
| 🔄 重新生成 | 可重新產生 AI 的最後一則回覆 |
| ✏️ 聊天室管理 | 新增 / 刪除 / 重新命名聊天室 |
| ⚙️ 設定面板 | 可自訂顯示名稱、偏好語言與回覆風格 |
| 📱 響應式設計 | 支援桌面與行動裝置瀏覽 |

---

## 🏗️ 技術架構

```
┌─────────────────────────────────────────────────┐
│                   前端 (Frontend)                 │
│         HTML + CSS + JavaScript (Jinja2)          │
├─────────────────────────────────────────────────┤
│                   後端 (Backend)                  │
│              FastAPI (Python 非同步框架)            │
├─────────────────────────────────────────────────┤
│                  資料庫 (Database)                 │
│             SQLite + SQLAlchemy ORM               │
├─────────────────────────────────────────────────┤
│                  AI 回覆模組                       │
│         Mock 模擬回應（預留 AI API 串接介面）        │
└─────────────────────────────────────────────────┘
```

- **前端**：HTML + CSS + JavaScript，使用 Jinja2 模板引擎
- **後端**：FastAPI（Python 非同步 Web 框架）
- **資料庫**：SQLite（透過 SQLAlchemy ORM 操作）
- **AI 回覆**：Mock 模擬回應（可日後替換為 Google Gemini / OpenAI API）

---

## 📂 專案結構

```
20260422/
├── app.py                 # 主應用程式（FastAPI 後端 + 路由 + 資料模型）
├── requirements.txt       # Python 套件依賴
├── database.db            # SQLite 資料庫檔案（自動生成）
├── templates/             # Jinja2 HTML 模板
│   ├── base.html          # 基礎模板
│   └── index.html         # 主頁面（聊天介面）
├── static/                # 靜態資源
│   ├── css/
│   │   └── style.css      # 樣式表
│   └── js/
│       └── main.js        # 前端互動邏輯
├── uploads/               # 使用者上傳的檔案存放目錄
└── docs/                  # 開發文件
    ├── PRD.md             # 產品需求文件
    ├── Architecture.md    # 系統架構文件
    ├── Models.md          # 資料模型文件
    └── Test.md            # 測試驗證文件
```

---

## 🚀 安裝與執行

### 前置需求

- **Python 3.10+**（建議 3.11 以上）
- **pip** 套件管理工具

### 安裝步驟

1. **進入專案目錄**
   ```bash
   cd 20260422
   ```

2. **安裝依賴套件**
   ```bash
   pip install -r requirements.txt
   ```
   或手動安裝：
   ```bash
   pip install fastapi uvicorn[standard] sqlalchemy jinja2 python-multipart aiofiles
   ```

3. **啟動應用程式**
   ```bash
   python app.py
   ```

4. **開啟瀏覽器**

   前往 👉 **http://127.0.0.1:8000**

> 💡 伺服器啟動後會自動建立 `database.db` 資料庫與 `uploads/` 目錄。
> 按 `Ctrl+C` 可停止伺服器。

---

## 📖 使用說明

### 基本操作

1. **建立對話**：點擊左側邊欄的「＋」按鈕建立新聊天室
2. **發送訊息**：在輸入框輸入文字，按 `Enter` 發送（`Shift+Enter` 換行）
3. **上傳檔案**：點擊輸入框旁的 📎 按鈕選擇檔案上傳
4. **切換對話**：點擊左側邊欄中的聊天室名稱進行切換
5. **重新生成**：對 AI 回覆不滿意時，可點擊重新生成按鈕

### 聊天室管理

- **重新命名**：右鍵點擊聊天室名稱可重新命名
- **刪除對話**：點擊聊天室的刪除按鈕可移除整個聊天室

### 偏好設定

點擊左下角「⚙️ 設定」按鈕，可自訂：
- **顯示名稱**：機器人會用您的名稱稱呼您
- **偏好語言**：繁體中文 / English / 日本語
- **回覆風格**：友善 / 正式 / 簡潔 / 詳細

### Mock AI 可辨識的問題

| 觸發關鍵字 | 範例 | 回覆內容 |
|-----------|------|---------|
| `你好`、`哈囉` | 「你好啊！」 | 友善的問候語 |
| `名字`、`你是誰` | 「你叫什麼名字？」 | 自我介紹並稱呼使用者 |
| `謝謝`、`感謝` | 「謝謝你！」 | 禮貌回覆 |
| `再見`、`掰掰` | 「掰掰～」 | 道別語 |
| 其他任意輸入 | 任何問題 | 隨機選擇預設回覆 |

---

## 📡 API 文件

啟動伺服器後，可存取自動產生的 API 文件：

- **Swagger UI**：http://127.0.0.1:8000/docs
- **ReDoc**：http://127.0.0.1:8000/redoc

### 主要 API 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| `GET` | `/api/sessions` | 取得所有聊天室列表 |
| `POST` | `/api/sessions` | 建立新聊天室 |
| `GET` | `/api/sessions/{id}` | 取得聊天室詳情與訊息 |
| `PUT` | `/api/sessions/{id}` | 更新聊天室標題 |
| `DELETE` | `/api/sessions/{id}` | 刪除聊天室 |
| `POST` | `/api/sessions/{id}/messages` | 發送訊息並取得 AI 回覆 |
| `POST` | `/api/sessions/{id}/regenerate` | 重新生成最後一則 AI 回覆 |
| `POST` | `/api/upload` | 上傳檔案 |
| `GET` | `/api/memory` | 取得所有使用者記憶 |
| `PUT` | `/api/memory` | 更新使用者記憶 |

---

## 📚 開發文件

本專案包含完整的開發文件，位於 `docs/` 目錄：

| 文件 | 說明 |
|------|------|
| [PRD.md](docs/PRD.md) | 產品需求文件 — 功能需求、使用者故事、驗收標準 |
| [Architecture.md](docs/Architecture.md) | 系統架構文件 — 技術選型、元件設計、資料流程 |
| [Models.md](docs/Models.md) | 資料模型文件 — 資料表設計、欄位定義、關聯關係 |
| [Test.md](docs/Test.md) | 測試驗證文件 — 手動測試清單、測試案例 |

---

## 🔮 未來規劃

- [ ] 串接真實 AI API（Google Gemini / OpenAI GPT）
- [ ] 支援 Markdown 格式的 AI 回覆渲染
- [ ] 新增深色模式切換
- [ ] 支援對話匯出（JSON / PDF）
- [ ] 新增使用者認證與多用戶支援
- [ ] 支援串流回覆（Streaming Response）
- [ ] 部署至雲端平台（如 Google Cloud Run）

---

## 📝 心得報告

**姓名**：陳柏宇
**學號**：D1155086

---

### Q1. 你設計的哪一個 Skill 效果最好？為什麼？哪一個效果最差？你認為原因是什麼？

在本次專案中，我總共設計了六個 Skill：PRD、Architecture、Models、Implement、Test、Commit。

**效果最好的 Skill：Implement（程式碼實作）**

Implement Skill 的效果最為顯著，因為它能夠根據前面產出的 PRD、Architecture、Models 文件，一次性生成完整且可執行的 FastAPI 應用程式，包含後端路由、資料庫模型、前端模板、CSS 樣式與 JavaScript 互動邏輯。生成的程式碼結構清晰，透過 `python app.py` 即可直接啟動運作，大幅縮短了開發時間。我認為效果好的原因在於，Implement Skill 的指令明確定義了技術架構（FastAPI + SQLite + Jinja2），並且有前面三份文件作為完整的需求脈絡，讓 AI 能精準理解要實作什麼功能。

**效果最差的 Skill：PRD（產品需求文件）**

PRD Skill 在初期使用時遇到了「No matching results」的觸發問題，無法被正確啟用。我認為原因是 Skill 的觸發條件設定不夠明確，導致 AI Agent 無法正確匹配使用者的輸入意圖。後來經過調整觸發關鍵字與描述後才順利運作。這讓我理解到 Skill 的設計不只是內容本身，觸發條件的精準度同樣至關重要。

---

### Q2. 在用 AI 產生程式碼的過程中，你遇到什麼問題是 AI 沒辦法自己解決、需要你介入處理的？

在使用 AI 產生程式碼的過程中，我遇到了以下幾個需要人為介入的問題：

1. **中文路徑編碼問題**：我的專案資料夾路徑包含中文字元（「生成式AI應用：AI agent 開發與實作(創能綜班)[0771]」），導致在終端機執行 `pip install -r requirements.txt` 和 `python app.py` 時出現找不到檔案的錯誤。AI 雖然能嘗試改用完整路徑或直接指定套件名稱來解決，但根本原因是作業系統對中文路徑的處理差異，這需要我自己理解並確認解決方案是否可行。

2. **GitHub 認證與儲存庫建立**：AI 無法代替我登入 GitHub 建立新的 Repository，也無法處理 Git 的身份認證（如 SSH Key 或 Personal Access Token 的設定）。這些操作必須由我手動在瀏覽器和本機環境中完成。

3. **個人化內容的判斷**：像是心得報告、姓名學號等個人資訊，以及對 Skill 效果好壞的主觀評價，AI 只能根據開發過程的客觀紀錄來推測，最終仍需要我本人確認內容是否符合實際感受。

整體而言，AI 在「產出結構化內容」和「執行重複性指令」方面表現非常出色，但在「環境配置差異」、「第三方服務認證」和「主觀判斷」這些面向，仍然需要開發者本人介入處理。

---

## 📄 授權

本專案僅供學習與教學使用。

---

> 📌 **課程**：生成式AI應用：AI agent 開發與實作（創能綜班）[0771]
> 📅 **日期**：2026-04-22
